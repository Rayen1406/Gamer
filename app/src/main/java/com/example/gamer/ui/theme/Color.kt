package com.example.gamer.ui.theme

import androidx.compose.ui.graphics.Color

// ---------- Light theme ----------
val LightPrimary = Color(0xFFF90043)   // text, buttons, toasts
val LightBackground = Color(0xFFFFFFFF) // screen bg
val LightOnBackground = Color(0xFF111418)
val LightSurface = LightBackground
val LightOnSurface = LightOnBackground
val LightError = Color(0xFFAC1209)      // field errors
val LightOnError = Color(0xFFFFFFFF)
val LightOutline = Color(0xFFE0E0E0)    // borders / outlines

// ---------- Dark theme ----------
val DarkPrimary = Color(0xFFF2055C)     // text, buttons, toasts
val DarkBackground = Color(0xFF07090D)  // screen bg
val DarkOnBackground = Color(0xFFEAECEF)
val DarkSurface = Color(0xFF0A0D12)     // slightly above bg for cards/snacks
val DarkOnSurface = DarkOnBackground
val DarkError = Color(0xFFB5261E)       // field errors
val DarkOnError = Color(0xFFFFFFFF)
val DarkOutline = Color(0xFF2C2F36)     // borders / outlines